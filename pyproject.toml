[project]
name = "understudy"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pydantic==2.13.5",
    "strands-agents==1.55.1",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
